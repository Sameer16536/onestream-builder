export const MAX_FILE_SIZE_MB = 50;
export const CHUNK_SIZE       = 5000;
export const WARN_ROWS        = 50000;
export const MAX_MEMBER_NAME  = 100;
export const VALID_EXTENSIONS = [".xlsx", ".xls"];
export const PREVIEW_ROWS     = 50;   // rows parsed for sheet preview (lazy)
export const PREVIEW_COLS     = 100;  // cols parsed for sheet preview (lazy)
export const PAGE_SIZE        = 25;   // rows per page in preview table
